// Tailwind v4 via PostCSS (the Next equivalent of @tailwindcss/vite).
// prettier-plugin-tailwindcss still reads ./src/index.css — see .prettierrc.
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
